# Runtime — pinned to tested versions; bump deliberately, not accidentally.
fastapi==0.139.0
uvicorn[standard]==0.51.0
anthropic==0.116.0
voyageai==0.4.1
pinecone==9.1.0
pydantic==2.13.4
pydantic-settings==2.14.2
# Draws the social card per question (app/og_card.py). The card is flat
# shapes and four lines of text, so this is here instead of a headless
# browser in the image.
pillow==12.3.0

# The clip renderer downloads just the requested seconds of an episode.
# Was dev-only while clipping was a local script; it is a runtime
# dependency now that the site can ask for a clip.
yt-dlp==2026.7.4
